The following string may be broken JSON. Fix it if possible.

Respond with JSON matching this JSON Schema:

```json
{{json_fix_response_schema}}
```

If it's more-or-less JSON, fix it and respond with the success response. If it's not, respond with
the failure response. Here's the string:
{{input}}
